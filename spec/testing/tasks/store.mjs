
import { WorkflowTask } from '../../../fore/domain/index.mjs';

export class Store extends WorkflowTask {

    constructor(workflow, name, fields) {
        super(workflow, name, fields);
    }

    run(presenter) {
        super.run(presenter);
    }

}

