[
    {
        "name": "wf1",
        "parameters": [
            {"name": "p1", "storage": "string"},
            {"name": "p2", "storage": "number"},
            {"name": "p3", "storage": "boolean"},
            {"name": "p4", "storage": "object"}
        ],
        "tasks": [
            {"name": "t1", "builder": "RunTest", "fields": ["p1", "p3"]},
            {"name": "t2", "builder": "PauseTest", "fields": ["p2", "p4"]}
        ],
        "steps": [
            {"name": "$start", "task": "t1", "jumps": [{"state": "ok", "target": "step2"}]},
            {"name": "step2", "task": "t2", "jumps": [{"state": "ok", "target": "$done"}]}
        ]
    },

    {
        "name": "run-test",
        "parameters": [ ],
        "tasks": [
            {"name": "run", "builder": "RunTest", "fields": [ ]}
        ],
        "steps": [
            {"name": "$start", "task": "run", "jumps": [{"state": "ok", "target": "$done"}]}
        ]
    },

    {
        "name": "pause-test",
        "parameters": [ ],
        "tasks": [
            {"name": "pause", "builder": "PauseTest", "fields": [ ]},
            {"name": "run", "builder": "RunTest", "fields": [ ]}
        ],
        "steps": [
            {"name": "$start", "task": "pause", "jumps": [{"state": "ok", "target": "resume"}]},
            {"name": "resume", "task": "run", "jumps": [{"state": "ok", "target": "$done"}]}
        ]
    }

]
