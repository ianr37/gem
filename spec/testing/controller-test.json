[
    {"run-test":
        {
            "parameters": [ ],
            "tasks": [
                {"taskName": "run", "taskClass": "RunTest", "fields": [ ]}
            ],
            "steps": [
                {"stepName": "$start", "taskName": "run"}
            ]
        }
    },

    {"pause-test":
        {
            "parameters": [ ],
            "tasks": [
                {"taskName": "pause", "taskClass": "PauseTest", "fields": [ ]}
            ],
            "steps": [
                {"stepName": "$start", "taskName": "pause"}
            ]
        }
    }

]
